import { Typography } from '@mui/material';
import MainLayout from '../../layouts/MainLayout';

const Create = () => {
  return (
    <MainLayout>
      <Typography variant="h2">Track upload</Typography>
    </MainLayout>
  );
};

export default Create;
